import CoreGraphics

/// Controls the overall size of the Key Monster panel and all its contents.
/// 1.0 = compact default; 1.5 = larger. Future candidate for a user preference.
let uiScale: CGFloat = 1.5
